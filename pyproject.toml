[project]
name = "regression-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SORAAAAAAAAA", email = "rcbriones95@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "kagglehub>=1.0.2",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
]

[project.scripts]
regression-model = "regression_model:main"
download-data = "data:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
